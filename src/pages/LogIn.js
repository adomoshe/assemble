import React, { Component } from 'react';

class LogIn extends Component {
    componentDidMount() {
      window.scrollTo(0, 0);
    }
    render() {
      return (
        <div>
<button>HI</button>
        </div>
      );
    }
  }

  export default LogIn;